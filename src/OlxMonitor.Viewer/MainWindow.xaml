<Window x:Class="OlxMonitor.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OLX Monitor Viewer" Height="700" Width="1300"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="SearchBox" Width="400" Height="32" Margin="0,0,15,0" FontSize="14"/>
            <Button x:Name="RefreshBtn" Content="Refresh" Width="120" Height="32" Click="RefreshBtn_Click"/>
        </StackPanel>

        <DataGrid x:Name="Grid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="180"/>
                <DataGridTextColumn Header="First Seen" Binding="{Binding FirstSeen, StringFormat=yyyy-MM-dd HH:mm}" Width="160"/>
                <DataGridTextColumn Header="Last Seen" Binding="{Binding LastSeen, StringFormat=yyyy-MM-dd HH:mm}" Width="160"/>
                <DataGridTemplateColumn Header="URL" Width="300">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Url}" Foreground="Blue" Cursor="Hand" MouseLeftButtonDown="Url_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>